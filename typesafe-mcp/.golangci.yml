version: "2"

linters:
  default: standard
  enable:
    - errorlint
    - gocritic
    - gosec

  exclusions:
    rules:
      - path: "_test\\.go$"
        linters:
          - gosec

formatters:
  enable:
    - goimports
