# mcp-gateway

Locally secure, deny-by-default MCP service and control foundation.

## Development

```bash
make build              # go build -o mcp-gateway ./cmd/mcp-gateway
make install            # go install ./cmd/mcp-gateway
make test               # go test -race ./...
make test-integration   # go test -race -tags=integration ./...
make test-e2e           # go test -race -tags=e2e -timeout=60s ./test/e2e/...
make test-keyring-native # schema-validated passed/skipped/failed native evidence
make accept-s2-1        # compatibility S2.1 gate from a clean checkpoint
make accept-s3          # compatibility S3 gate from a clean checkpoint
make accept-s4          # complete S4 gate from a clean checkpoint
make lint               # go tool golangci-lint run ./...
make fmt                # go tool goimports -w .
make tidy               # go mod tidy && go mod verify
make audit              # tidy + fmt + lint + test + advisory govulncheck

go tool govulncheck ./... # blocking vulnerability check
```

Run `make audit` before committing, and use the unsuppressed `go tool govulncheck ./...` command when vulnerability findings must block. Unit tests are colocated and race-enabled. Integration tests use `//go:build integration`; real-binary tests use `//go:build e2e` under `test/e2e/`. Run `make accept-s4` only from the final clean tracked checkpoint; `make accept-s3` and `make accept-s2-1` retain compatibility profiles. Do not bypass any profile's required-clean policy or suppress a failing command. The closed schema-validated S4 report must retain the profile, exact revision, dirty state, command/criterion/evidence/artifact mapping, contract-owned AC-1–AC-5 and normative-clause manifests, and nested native classification. Every manifest evidence name must be preassigned to a deterministic command; native `skipped` is additive, while native `failed` blocks.

## Package layout

```text
cmd/mcp-gateway/        Cobra composition root only
internal/composition/   Production runtime/catalog graph construction and cycle binding
internal/contract/      Immutable S1–S4 routes, problems, limits, states, representations, and acceptance manifests
internal/strictjson/    Bounded strict JSON, lexical value trees, compact encoding, and canonical equality
internal/paths/         Owner-only paths and process ownership
internal/storage/       SQLite identity, migrations, durability, latch
internal/authorization/ Online S3 principal, credential, grant, and policy SQL authority
internal/grantrequests/ S5 policy, evidence, dedupe, and request workflow ownership
internal/selfservice/   S5 synthetic catalog, safe projections, protected cursors, and local handlers
internal/servers/       Durable S2 identities, desired authority, operations, cursors, idempotency
internal/catalog/       Durable descriptors, traversal/normalization, active snapshots, routes
internal/credentialauthority/ Current server credential resolution
internal/servercredentials/ Static and OAuth-client replacement cutover
internal/discovery/     Principal-specific current-tool projection, cursors, and page packing
internal/runtimes/      Process-local reconciliation plus bounded direct stdio supervision
internal/remote/        Shared canonical URL, address policy, pinned dial, and HTTP exchange
internal/oauth/         Bounded protected-resource and issuer trust graph
internal/downstream/    Raw bounded JSON-RPC plus stdio/Streamable HTTP connections
internal/invocation/    S4 redaction, result projection, audit, and call orchestration
internal/admin/         Admin bearer and in-memory session authority
internal/api/           Strict resources, JSON, and safe problems
internal/httpboundary/  Listener, route ownership, and early validation
internal/events/        Bounded invalidation-only delivery
internal/limits/        Reserved package marker; concrete gates remain component-owned
internal/mcpingress/    Auth-first modern and legacy MCP adapters
internal/keyring/       Typed capability and opaque generations
internal/backup/        Verified backup and stopped restore
internal/lifecycle/     Startup, readiness, drain, and shutdown
internal/dependencies/ Build-tagged dependency pin only
internal/testutil/      Deterministic clocks, entropy, roots, scans, and processes
test/acceptance/        Clean-checkpoint acceptance runner and report schema
test/e2e/               Single real-binary harness and bounded fixtures
test/keyringnative/     Isolated native keyring evidence
test/material/          External-package material acquisition evidence
```

`internal/dependencies` is only the build-tagged dependency pin. Production files must not import `internal/testutil`; it is shared only by tests.

## Invariants

- `internal/contract` is the only source for S1–S4 routes, problems, limits/deadlines, media/protocol values, resource/union shapes, closed states/reasons/events, mechanics, approved secret sinks, and acceptance manifests; do not duplicate them in later packages.
- `internal/strictjson` is the dependency-neutral parser for API, downstream, OAuth, and catalog boundaries. Supply explicit positive size/depth bounds; closed destinations reject unknown members. Its separate value tree preserves lexical number tokens and object order without changing canonical equality. Do not reintroduce package-local permissive decoders or equality that depends on object member order.
- Exact default authority is `127.0.0.1:8210`; do not broaden accepted bind or Host forms. Keep validation and route classification ahead of authentication/body work, and preserve the control-auth to admin-work nonblocking permit transfer.
- `internal/composition` is the sole production constructor for the authorization, discovery/cursor/pager, runtime, catalog, keyring-material, and OAuth graph. Constructor, deterministic provider/protocol fixture, and startup-barrier seams stay package-private; ordinary non-`e2e` construction always selects the native keyring backend. Every cycle callback rejects or does nothing safely until the complete graph is atomically bound. Its one-shot `Start` requires control-plane readiness, binds before any reconstruction, and lists all durable servers before starting per-server work. Runtime, traversal, active-tool, and dispatch occupancy come from their actual owner objects, never placeholder or summed duplicate counters. Its idempotent drain publishes one joinable result, synchronously rejects new authority work, cancels pending leases, invalidates discovery generations, and withdraws active routes before it fences catalog/OAuth/keyring callbacks, stops all runtime phases concurrently, and waits outside authority/catalog locks for quiescence within the caller's deadline; an expired drain remains unclean while late completions stay fenced from storage, publication, and invalidation.
- `internal/composition/source_test.go` owns repository-wide production guards and fixture-driven exact path/symbol failures. It requires one composition-owned positive authenticator/list/status bundle and forbids ordinary root deny-all; only ingress handler ownership and the build-tagged dependency pin may import the MCP SDK. S3 SQL belongs only to `internal/authorization`, migration `008_authorization.sql`, and `invalidateAgentCredentialCandidate` in stopped recovery. Discovery may depend only on safe authorization/catalog/contract facts: no routes/capabilities, keyring/runtime/process/network/HTTP/SDK owners, list caches/subscriptions, raw-secret/session/history columns, or control cursors. No production audit read API, agent self-service tool, UI, replay path, or second active-capability consumer may emerge; governed calls belong only to the composed invocation service. Existing process/HTTP/transport/runtime-column guards remain path- and symbol-specific; the sole test-tree process-construction exception is `test/acceptance/acceptance.go`, which executes only its closed final-gate table. Extend this guard when a production ownership boundary is added; never weaken it to make an unrelated refactor pass.
- API JSON must remain size/depth bounded and reject invalid UTF-8, duplicate members, unknown members, and trailing values. Every API response is `no-store` and no response enables CORS.
- Keep admin and agent credential domains, middleware, identifiers, and invalidation channels separate.
- Production MCP authentication uses the composition-owned principal credential authority and discovery adapter. Keep positive tests on that same lease-producing boundary; never introduce a second authenticator or authority owner.
- Raw secrets never enter SQLite, configuration, argv, logs, metrics, URLs, events, backups, browser storage, or read APIs.
- SQLite security mutations arm durable intent before beginning and fail closed on uncertain outcomes. No online repair or replay.
- Keyring startup probing is secret-free and presents no prompt. Get/Set/Delete may interact or outlive cancellation as a documented MVP limitation; admit only one globally, retain its slot until return, and never fall back to plaintext.
- Limits are compiled constants, acquired in the documented order, and reject without queuing. Event streams release authenticated-admin admission after authentication and retain only their own bounded stream/buffer permits so they cannot starve status or recovery.
- SSE is invalidation-only: no IDs, replay, cursor, secret, or authority. Overflow disconnects the stream; clients recover through authenticated snapshot reads.
- The first process signal drains and closes ephemeral registries within the fixed deadline; the second forces exit. Drain the keyring coordinator before closing storage so late context-free results cannot commit.
- `internal/servers` owns desired-server, authority, operation, auth-flow, cursor, and idempotency SQL; `internal/catalog` owns durable catalog SQL and process-local active publication/routes. The servers package's sanitized contract-typed desired input cannot carry secret payloads; SQLite may contain only safe desired/tombstone facts, independent authority revisions and opaque handles, operation and auth-flow history/watermarks, and safe-reference idempotency metadata. Cross-domain grant validation uses only its supplied-transaction `ValidateGrantTargetTx` seam; callers never query S2 tables or open nested mutation admission.
- `internal/storage` owns S3–S5 migration DDL, singleton seeds, structural checks, historical fixture migration, and the exact raw-bearing-column exclusion only. Its sole S3 DML exception is stopped recovery of a marker-named agent credential candidate: clear only the exact current principal/credential/revision tuple, advance principal and credential revisions once, and never restore authority. Online S3 SQL and semantic row validation belong to `internal/authorization`; online S4 invocation SQL belongs to `internal/invocation`; online schema-10 request identity, creation, dedupe, retention, and evidence-accounting SQL belongs to `internal/grantrequests`. Do not grow storage into any repository.
- `internal/authorization` is the sole online owner of S3 principal, current-credential, grant, synthetic-identity, and authorization-revision SQL. Safe control reads use coherent transactions, insertion-watermark cursors bound to exact filters, clock-derived grant expiry, and typed fail-closed storage errors; they expose no verifier bytes. Reject a latched store before, during, or after every read. Before constructing the rest of the production graph, validate every bounded S3 row and singleton in one transaction, delegating stored S2 target existence and synthetic collision checks to the supplied-transaction servers seam. Compile constraints only through the closed bounded equality compiler: retain lexical number tokens, decode RFC 6901 into object-only segments, allow empty/numeric/prefix-paired paths, and never use canonical equality, arrays, coercion, schema interpretation, or overlap analysis. Principal creation, its ordinary permanent synthetic ALLOW, and one authorization-revision advance share a single mutation; PATCH increments principal revision once, increments authorization revision only for state transitions, advances credential revision and clears authority on disable, and never restores authority on re-enable. Agent issue/replace uses independent `agent-verifier/v1` and `agent-fingerprint/v1` SHA-256 domains, returns the bearer only after acknowledged candidate-marker publication, and advances principal/credential revisions without policy; explicit revoke clears the singular slot under an ordinary marker, and uncertain candidate recovery never restores prior authority. Authentication must validate the canonical agent domain, load and validate every bounded current candidate in one latch-aware transaction, and compare all verifiers with `subtle.ConstantTimeCompare` without a verifier SQL predicate or early return. One repository-owned nonblocking gate makes successful authentication return an already-registered pending lease with only safe binding fields and orders every online principal, credential, grant, and authorization-revision mutation as gate → `Store.Mutate` → transaction work → post-commit invalidation → release. Principal/credential commits and latch uncertainty cancel only affected pending leases; grant commits never close credential channels and depend on transaction re-evaluation. Gate and storage saturation reject rather than wait. S4-facing admission acquires only the gate: its caller owns `Store.Mutate` and passes the existing `*sql.Tx`; never nest mutation admission. Resolved verification returns a closed phase: only `binding_verified` plus the non-storage semantic unavailable error may produce a policy-free `authorization_unavailable` audit row; binding, context, SQL, latch, and transaction failures produce none. Verify exact active credential binding first, then either evaluate the current revision/route/unchanged token tree at one authority-clock timestamp captured inside the transaction or return policy-free binding-only revision/time evidence. Advisory revision mismatch always re-evaluates. Only a pending ALLOW token confirmed after acknowledged commit may transition its lease to admitted and remove it from invalidation; rollback, uncertainty, cancellation, or late confirmation cannot detach. That token then exposes one sealed immutable admitted subject containing only principal/credential IDs and revisions plus the evaluated authorization revision—never bearer, verifier, fingerprint, or visibility. Authorization-owned self projections accept only that repository-bound subject, reverify current principal/credential revisions in the same coherent read as the principal's insertion-watermarked ordinary grants, derive active/expired state at one clock instant, and resolve synthetic/extant/deleted immutable target namespaces only through the servers-owned supplied-transaction inspector. They expose no principal selector, internal server ID, credential authority, or partial page. Release/cancel is idempotent; drain fences, removes and cancels pending leases outside the registry lock, then boundedly waits for gate quiescence. Stopped recovery needs no live gate under exclusive process ownership, and the registry has no goroutine, timer, queue, or alternate owner. Authorization-owned staged invalidation accepts only a supplied verified schema-9 store plus the S2 target inspector; in one `Store.Mutate`, fully validate authority before and after clearing every complete current slot and advancing only affected principal/credential revisions. The staged invalidation seam preserves timestamps, authorization/synthetic/Gateway revisions, S2 facts, and admin authority; backup orchestration then rekeys admin authority. It calls this seam after migration and initial verification for every accepted schema 3–9 stage, then checkpoints/closes and reruns SQLite plus complete authorization semantic verification before install; it must never duplicate S3 SQL. Expose the positive lease-producing authenticator to root only through composition's atomic agent-ingress dependency bundle. The principal control API consumes only the composition-owned repository: its dedicated query parser rejects unknown, repeated, empty, or null members; create/read/PATCH emit exact principal ETags; PATCH members are non-null and nonempty as a set; successful writes emit one ID-free authorization invalidation only after commit, and the singular credential POST/DELETE accepts exact `{}` plus the principal ETag. Only successful POST returns the raw bearer, exactly once; replacement/revocation failures and retries expose no prior or candidate authority. Grant API list/create/read/delete uses the same repository and the root-supplied S2 transaction callback; create requires all nullable members to be present, list cursors bind exact principal/server filters, reads are bodyless, and successful writes emit only the ID-free authorization invalidation. Ordinary grants are immutable create/delete rows; creation compiles constraints before admission, validates a non-deleted target through the S2 callback inside the same transaction, counts expired rows toward capacity, and advances authorization revision exactly once without descriptor, history, provenance, reason, idempotency, or ETag machinery. Evaluate one bounded token tree at one timestamp in one coherent revision/grant read, validate every loaded row, apply DENY then ALLOW then BLOCK, and expose only the smallest matching grant ID; malformed arguments, policy, traversal, or storage fail closed without partial evidence. The credential-bound discovery policy seam verifies the pending lease and exact current binding, then returns one revision plus only active structural grant scope/effect/constraint-presence at the caller-supplied timestamp; it validates every loaded row and never exposes IDs, constraints, predicates, or evidence.
- `internal/mcpingress` authentication consumes the concrete already-registered authorization lease; never re-copy its binding into an ingress authority type or restore optional post-auth subscriptions/credential-expiry assumptions. One shared request owner lets the HTTP boundary release post-auth aborts and ingress release every terminal path; modern request contexts cancel from lease invalidation. Successful legacy initialization alone transfers that lease into a process-local session, which matches principal ID, credential ID, and credential revision, watches `Lease.Done`, and releases once on DELETE, invalidation, idle/absolute lifetime, failure, shutdown, or restart. Reauthenticated existing-request leases remain request-scoped. The shared raw list codec owns exact ID-preserving discovery envelopes and strict absent/empty/cursor-only application params. The adjacent call codec accepts only a non-null string/number request ID, strips `_meta` and every disallowed param member before its era-neutral service seam, retains token-preserving name/arguments plus a typed wire-validity bit for auditable invalid params, excludes notifications/batches, and owns only the five fixed safe call envelopes. Modern injection passes its registered request lease and cancellation context to this seam before stateless SDK dispatch. Legacy injection first matches the exact initialized session binding, then passes only the reauthenticated request lease and cancellation context; it never substitutes or releases the session-owned lease. Intercept every non-lifecycle feature method before the SDK, retain SDK ownership only for the closed bootstrap/session methods, and write owner-supplied final encoded bytes without re-marshalling. The discovery-independent list interface is the only ingress adapter seam: injection makes both eras advertise exactly `tools:{}`, passes the request lease/cursor/cancellation, and maps failures to closed safe errors; a legacy list must first match the exact session binding and must never use or release the session-owned lease as its request authority. Do not import discovery and recreate the catalog test cycle. Production root consumes the composition-owned authenticator, list adapter, call adapter, and `principal_credentials` status from one dependency bundle.
- `internal/discovery` combines that single SQL policy view with one cloned current-catalog snapshot. It owns deterministic external-name/tool-ID ordering, exact closed MCP Tool projection, structural `all`/`requestable`/`allowed-only` filtering, and one generation recheck without retry. `all` includes every current tool; `requestable` excludes an applicable unconstrained DENY; and `allowed-only` requires an applicable ALLOW—constrained ALLOW counts structurally—and no applicable unconstrained DENY. Its distinct `mgw_dc1_` cursor is a fixed-width versioned raw-base64url frame authenticated with a process-local 32-byte HMAC key; verify the MAC before interpreting payload fields, bind the full snapshot/method/next position, classify malformed syntax separately from stale/tampered binding, and never persist or expose the key. Its narrow `gosec` conversion suppressions cover only the reversible signed-second sign-bit mapping, `time.Time`'s bounded nanosecond component, and bounded page-position conversions; do not broaden them. Page packing accepts one deterministic compact encoder for the complete JSON-RPC response, sizes standard Tool encodings against empty final/nonfinal envelopes, preflights cursor-free final pages, returns the exact verified bytes, caps only at 100 tools/4 MiB, and never applies the downstream 32-page traversal limit. Encoder disagreement or failure is not a resource-limit error; resource limit means the first projected tool cannot fit. The package must not import or acquire routes, capabilities, bindings, runtimes, network/process owners, SDK list caches, or subscriptions, and projected results contain no policy advice or administrative descriptor fields.
- Every standalone server-domain mutation uses `storage.Store.Mutate`. Keyring publication/invalidation is the sole exception: a repository callback updates authority metadata directly on the coordinator-owned existing `*sql.Tx` under captured desired, credential, optional registration/flow, and drain fences. That callback must never call `Store.Mutate`. Keep keyring, process, network, OAuth, initialization, discovery, and transport work outside mutation admission; map a latched store fail closed and never repair online.
- Runtime state is never serialized or resumed after restart. Direct stdio execution uses only a validated absolute executable, literal arguments, exact working directory, clean declared/runtime-secret environment, and a fresh process group; ownership and retained diagnostics stay process-local and raw output never reaches public or durable sinks. Stop closes input, revalidates the captured process group before TERM and KILL, uses exact 3/2-second graceful/forced windows, and reports success only after reap; never signal an unverified PID or claim hard-crash orphan prevention. Concrete constructor and runtime failures use one closed classifier for state, reason, retry eligibility, and runtime-loss evidence. A selected handle reports only its first fatal process, output, protocol, authentication, session, or transport failure; deliberate stop suppresses reporting. Reconciliation accepts a report only for the exact activating or active candidate, withdraws before stop, and rejects duplicate or stale reports without affecting a replacement. Catalog invalidity, limit, or request failure remains catalog-only while transport health is proven; typed authentication, session, process, protocol, transport, or ownership loss requires runtime withdrawal. Behavioral work synchronously fences publication, withdraws before stop, starts only after confirmed cleanup, conditionally publishes the current generation, and marks operation success last; unconfirmed cleanup retains its exact handle and gates replacement. Shutdown uses the separate all-runtime drain: synchronously fence/withdraw all, then stop every owned runtime concurrently outside global-four reconciliation, drain keyring consumers before storage closes, and never mark the run clean when stop proof is unavailable.
- Test subprocesses use `testutil.BinaryRunner` with a positive configured timeout and per-stream byte cap. It captures stdout/stderr separately, reports truncation, and owns a fresh process group through identity-validated, bounded TERM/KILL/reap cleanup; production runtime supervision remains a separate component owner. Real-binary tests use the single harness in `test/e2e/harness_test.go`, which builds with `-tags=e2e`, initializes admin authority, owns start/stop and bounded HTTP, and supplies admin/event/operation plus principal/credential/grant/current-catalog/raw-MCP helpers. The `e2e` tag replaces only the composition provider factory with the deterministic in-memory backend; ordinary builds select `keyring.NewProvider`, and neither selection has public configuration. Keep binary era cases representative (stdio and hardened HTTP modern plus fresh auto fallback) while retaining every fallback class in the concrete-driver package matrix; stdio fixtures write PID/request/stop evidence to bounded test-owned files rather than stdout, which remains the protocol channel, and loopback HTTP fixtures use raw JSON-RPC with explicit header/session/connection observations rather than an SDK client.
- `internal/remote` is the only production owner of outbound `http.Client`/`http.Transport` construction. It validates canonical destinations, resolves fresh and pins the selected validated address at dial, retains platform TLS hostname verification, and disables proxies, redirects, cookies, compression, keepalive reuse, and convenience APIs. Downstream and OAuth roles must use this factory and add stricter role policy rather than constructing clients.
- `internal/oauth` owns the exact registered-resource trust anchor, RFC 9728 endpoint/root discovery, issuer selection, RFC 8414/OpenID order, strict extensible metadata projection, canonical explicit trusted-origin policy, and static/dynamic registration. A present challenge metadata URL never silently falls back; public reachability never selects an issuer; cross-origin or multiple issuers require the exact desired pin. Only the resource origin and explicit trusted DNS origins may permit restricted machine-fetch addresses. DCR is one unauthenticated request, selects Basic then Post then None, and publishes confidential client authority only through a marker-armed keyring/repository callback. Foreground flow preparation persists only safe lifecycle metadata; state, verifier, endpoints, and requested scopes stay process-local, while the authorization URL appears once in the creation response. Registration publication is additionally fenced by the current preparing flow. Callback handling admits eight globally before consuming state, validates issuer before either branch, and makes one exact bound token request. Initial complete token authority uses post-authorization-success cutover; keyring activation and flow success share one transaction, while every persistence fault invalidates authority. Write-only static/OAuth-client replacement validates safe shape and revisions before decoding values, uses ordinary old-or-new complete-generation cutover, and atomically records one credential revision plus operation. Refresh holds one admitted keyring operation across current token/client reads, exact hardened HTTP, and the shared post-AS-success cutover; uncertain after-send or invalid rotation invalidates authority and never retries a tool call. Hardened downstream HTTP projects only bounded recognized Bearer challenge fields. Modern discovery, legacy initialization, and the first raw `tools/list` page terminate with a typed credential-replacement disposition and zero inline refresh/replay. Reconciliation may consume one recognized invalid-token disposition for the exact generation: refresh uses the captured authority fence without generic status/trigger callbacks, then withdrawal and verified stop precede exact current-authority reacquisition and a fresh runtime that replays only the challenged stage once. Insufficient scope is projected for foreground step-up, while later-page or second challenges become terminal authentication failures. Traversal owns raw bounded pages/cursors/names and isolated entries without SDK list caches, subscriptions, or durable/publication effects. Normalization projects closed fields, compiles object-root Draft 2020-12 schemas with local refs and a rejecting external loader, materializes annotation defaults, JCS-canonicalizes/fingerprints output, and retains only unique typed modern-HTTP header bindings with reviewed SEP-2243 scalar encoding. Durable catalog commit checks every desired/authority/catalog fence and both identity capacities before mutation, preserves immutable IDs, retires only on success, and exposes revision-bound descriptor resources without persisting active facts. The active registry owns one process-global reservation/publication lock, process-specific generation, immutable active snapshots, and generation-bound aggregate pages; never move active state or header bindings into SQLite. Its discovery read DTO clones only descriptors from exactly `current` servers plus separate process/active generation evidence; stale/absent/unavailable facts, bindings, routes, capabilities, runtime handles, and mutable references never cross that seam, and callers must recheck the generation once before return. Catalog polling uses the injected strict epoch grid, coordinator-owned per-server singleflight, and the Traverser's global-four admission. Explicit refresh attaches its operation to exact poll work; a challenge completion is handed to the manager only after singleflight removal, and one admitted cutover retains that operation while an unattached poll has none. Never restart lifecycle, duplicate the consumer, or queue another traversal. Active route capabilities are opaque, revision-pinned, revalidated at acquire and execute, and removed/fenced before lease cancellation. The same lock-coherent publication retains immutable compiled input validators; direct external-name resolution returns one safe descriptor-evidence snapshot, its validator, and the exact capability without consulting discovery or acquiring dispatch capacity. Header providers must use only normalized bindings and validated arguments. Disconnect/delete invalidate local authority before one-shot best-effort RFC 7009 work and local generation deletion; `cleanup_pending` retry is local-only and can never replay revocation or restore authority. Reconstruction validates only transport-required current authority and preserves all keyring capability classes; it stays asynchronous after readiness. Draining suppresses late credential status, fences runtime publication and keyring epochs, and runs before storage closure without a cancellation-bounded native-keyring claim.
- `internal/invocation` owns fixed recursive argument redaction, the closed era-neutral call result boundary, all online schema-9 invocation SQL, and the one-shot internal call service. It prepares one immutable ID/time before mutation, validates every stored row at startup, inserts admission evidence with oldest-first 4,096-row eviction in the same caller-owned transaction, and permits only one best-effort terminal compare-and-set; zero rows after eviction or prior annotation are benign. Reads are latch-aware, entropy is serialized, and canonical UTC timestamps, complete nullable groups, grant/decision coherence, chronology, and compact captures are revalidated semantically. The service classifies params, resolves one immutable catalog target, validates and redacts the unchanged token tree, admits through the one S3 gate/mutation path, and acquires/executes the pinned capability once only after acknowledged ALLOW detachment. Production wraps it in one invocation-owned nonqueueing pipeline fence/counter retained by composition; its permit encloses the complete synchronous service call through terminal annotation, and the process-local owner changes no public status shape. It never resolves again, holds authority or mutation admission during dispatch, or retries after any outcome; terminal annotation failure cannot alter the live result. The result boundary accepts only the five specified content unions, strips protocol `_meta`, preserves arbitrary validated `structuredContent`, converts tool/JSON-RPC/invalid results to safe contract codes, and classifies transport certainty only from downstream's typed failure evidence; raw errors and unsuccessful content never cross its outcome seam. Modern `resultType` is accepted only as `complete` and is stripped; input-required work is unsupported and never retried.
- `internal/downstream` owns raw JSON-RPC IDs/envelopes, exact outbound MCP application headers, bounded JSON/SSE bodies, stdio/HTTP connection closure, exact modern/legacy/auto negotiation, fresh verified fallback, runtime-local immutable legacy sessions, one-shot call construction, transport-specific cancellation, the pre-start/start-uncertain handoff marker, complete-response `response_invalid` evidence, and opaque immediate current-runtime capabilities. Fixed downstream identity is `mcp-gateway/s2`. Capability acquisition is global 32 then server four, revalidates every bound revision after both permits, releases server before global, and never waits. Only the composition-owned invocation service may consume it through the atomic ingress call adapter; no ingress or root path may access it directly. Modern evidence never downgrades; modern HTTP is sessionless and never posts cancellation; legacy session loss closes the runtime. The selected runtime retains the first fatal request failure and the exact result of its first close attempt so a later owner cannot mistake an unconfirmed close for verified cleanup. Mark immediately before OS write/RoundTripper entry and never infer attempt state from returns. Do not use SDK `Client.Connect`, `CommandTransport`, automatic cancellation/retry/reconnect, cached list paths, subscriptions, or list-change machinery.
- The official MCP SDK sits behind Gateway-owned authentication, classification, binding, limits, and lifecycle.
- Production composition owns and startup-validates exactly one S3 authorization repository and one S4 invocation repository before constructing the listener. Root consumes only that owner; the control API and live principal/grant occupancy use its narrow accessors. Composition synchronously fences invocation admission first, then authority and active routes, and includes admitted invocation pipelines through terminal-write completion in bounded authority/runtime/keyring quiescence before storage teardown. The current executable also production-composes exact downstream authority acquisition, one mixed runtime owner, concrete stdio and hardened HTTP construction, modern/legacy/auto negotiation, failure fencing, OAuth refresh/replacement, bounded catalog traversal, durable descriptor evidence, one active registry, internal revision-pinned routes, actual-owner status, and ordered deadline-bounded drain. `executeServe` constructs this graph before opening the listener and starts reconstruction only after readiness. Production `/mcp` authenticates principal credentials, lists projected descriptors, and routes governed calls through the sole composed invocation service; root source guards require one authenticator/list/call/status bundle and exact invocation constructor, SQL, capability-acquisition, and wire-owner allowlists while prohibiting direct root capability access. ID-free authorization invalidations and principal/grant occupancy are control-plane facts only. Production implements current principal authentication, `tools/list`, and governed `tools/call`; do not infer an audit read API, Gateway-owned self-service tools, replay, exactly-once effects, guaranteed secret detection, or product-UI behavior. Fixed redaction is least-disclosure defense in depth, and explicit caller retry after `outcome_unknown` may duplicate an effect.

## Dependency flow

`cmd/mcp-gateway` wires concrete adapters into narrow package-owned interfaces. Keep storage, keyring, HTTP, and protocol dependencies behind their owning packages; do not create a shared monorepo module. Use injectable clocks, entropy, sinks, runners, authenticators, and validators only where the owning behavior requires deterministic tests.
