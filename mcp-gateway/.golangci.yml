version: "2"
linters:
  default: standard
  enable:
    - errorlint
    - gocritic
    - gosec
  settings:
    gosec:
      excludes:
        - G304 # Security-sensitive paths are validated before file access.
  exclusions:
    rules:
      - path: _test\.go
        linters:
          - gosec
formatters:
  enable:
    - goimports
