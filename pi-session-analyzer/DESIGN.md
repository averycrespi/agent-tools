# Design

`pi-session-analyzer` is a passive, local-only pipeline from Pi JSONL logs through credential scrubbing into private SQLite storage, deterministic findings, CLI summaries, and bounded read-only MCP queries.
